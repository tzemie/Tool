# VPN 配置、规则与工具维护规则

## 1. 目录用途

- 本目录对应 GitHub 仓库 `tzemie/Tool`，维护可公开分享的 VPN 配置、分流规则、工具脚本及说明。
- 现有客户端目录为 `Loon/` 和 `Quantumultx/`；保留目录拼写、文件路径及可被客户端订阅的地址。
- 配置语法与行为以目标客户端为准；Windows / PowerShell 规则仅适用于相关脚本，不套用于其他客户端。
- VPN 协议、服务器和新增工具以具体任务为准，不预设技术方案。
- 修改聚焦用户指定的工具和问题；保留已有脚本、配置和用户改动。

## 2. 文件组织

- 按工具或独立用途建立子目录，使用简短、可辨识的名称。
- 小工具可将脚本和 `README.md` 放在同一子目录；复杂后再增加 `scripts/`、`config-examples/`、`tests/`。
- 公共逻辑确有多个使用方时再提取，避免为简单脚本引入框架或重复副本。
- 文件名说明用途，例如 `Test-VpnConnection.ps1`；不使用 `最终版2` 等无法判断版本的名称。
- 引用或修改第三方配置、规则时保留原有来源和署名；调整已有公开路径前检查引用关系。
- 下载的客户端安装包、运行缓存和临时诊断输出默认不放入本目录。

## 3. OneDrive 与敏感信息

- 本仓库为公开仓库，提交的内容按任何人可读取处理；本地目录位于 OneDrive 时，还应考虑其设备同步范围。
- 密码、令牌、私钥、含凭据或私人连接信息的订阅链接、恢复码和包含这些内容的真实连接配置，不写入脚本、文档或配置示例。
- 优先使用客户端自身的凭据存储、Windows Credential Manager 或同步目录之外的本地配置；脚本仅保存引用方式。
- 凭据、机器专属状态、配置备份和运行日志默认保存在 `%LOCALAPPDATA%\VPN-Tools\<工具名>\`；明确记录实际位置。
- 如用户明确要求同步真实配置，先说明具体文件包含的敏感信息和同步范围，再按已授权范围处理。
- 日志和报错脱敏，不输出完整令牌、私钥、含凭据或私人连接信息的订阅链接或未经筛选的配置正文。
- Git 忽略规则只是额外保护；不能阻止 OneDrive 同步，也不能代替上述存放规则。
- 公开文档不记录私人项目或聊天链接、完整个人工作路径、设备身份、内网拓扑或原始诊断日志；只记录必要的技术结论。

## 4. 脚本编写

- 修改客户端配置前确认对应格式、段落、策略组和引用关系；不同客户端之间迁移时逐项核对兼容性。
- 涉及 Windows / PowerShell 的工具，在说明中标明 PowerShell 版本、依赖和是否需要管理员权限；不默认所有设备环境相同。
- 路径使用 `$PSScriptRoot`、参数或明确的用户配置，避免硬编码用户名、盘符和网卡名称。
- 文件操作优先使用 PowerShell 原生命令和 `-LiteralPath`，兼容空格与中文路径。
- 不覆盖 `$HOME`、`$CODEX_HOME` 等系统变量；不拼接未经验证的输入作为命令执行。
- 执行前检查依赖和目标是否存在；外部命令检查退出码，失败时给出具体原因。
- 网络请求和重试设置合理上限；成功与失败应有可识别的输出和退出码。
- 修改系统配置的脚本应能重复运行而不制造重复规则，并提供预览能力，例如适用时使用 `-WhatIf`。
- 只对需要的操作请求管理员权限；不擅自修改全局 Execution Policy 或关闭安全软件。

## 5. 网络变更与回退

- 先检查当前状态，再实施修复；区分 VPN 连接、DNS、路由、系统代理和目标服务的问题。
- 修改 DNS、路由、代理、防火墙、网卡、服务或自启动前，说明具体目标、预期影响和回退方法。
- 备份仅保存本次将修改的必要状态，记录时间和位置；备份含敏感内容时按第 3 节存放。
- 用户已经授权的相关操作继续完成，无需重复确认；仅要求整理规则、评估或编辑脚本时，不执行实际网络变更。
- 如下一步超出授权范围，或将中断当前远程连接且尚未获得相应授权，应先把具体操作和恢复方案整理清楚，再询问用户。
- 回退针对本次改动，避免覆盖用户在此之后新增的配置；无法自动恢复的步骤要事先说明。
- 不默认进行全局网络重置、批量关闭进程、卸载客户端或删除历史配置。

## 6. 验证标准

- 按改动选择验证方式：文档检查、语法检查、参数校验、预览或实际运行；不为简单文档修改增加测试工程。
- 需要自动化测试时，验证有意义的行为，尤其是失败处理、重复运行和配置恢复。
- 静态检查通过只说明相应检查通过，不能据此宣称 VPN 已连通或故障已修复。
- 实际联网验证使用用户指定或与任务直接相关的目标，并记录检查项目、时间和结果。
- 结论注明已验证内容及未验证原因；截图、日志或配置存在本身不能证明修复有效。

## 7. 工具说明与交付

- 每个可用工具的 `README.md` 写明用途、环境要求、运行命令、主要参数、是否改变系统状态，以及适用的回退方法。
- 示例使用占位符或脱敏数据；涉及 Windows 界面时使用英文 UI 标签。
- 交付简要说明修改了什么、文件在哪里、如何使用、验证结果及仍存在的限制。
- 只有文档和草案时，明确说明尚未实现或运行，不将计划描述成可用功能。

## 8. 后续维护

- 用户在当前任务中的明确要求优先于本文件；已有授权在任务内持续有效。
- 新增规则应源于实际需求，保持简短；某个工具特有的约定放到该工具目录的说明中。
- 修改前读取相关脚本和说明，优先复用现有实现；需要整理历史文件时先确认目标、冲突和恢复方式。

## 9. GitHub 与 ChatGPT 资料一致性

- 仓库 `main` 分支是两端共享资料的正式版本。本地提交后须成功推送，GitHub 才获得更新。
- 开始工作时读取本文件和 `PROJECT_CONTEXT.md`，检查本地改动、分支与远端版本；有分歧先比较、合并，禁止静默覆盖或强制推送。
- 把已确认且适合公开的讨论结论写入 `PROJECT_CONTEXT.md`，区分决定、建议、已实施和已验证状态。
- 修改后检查差异，逐项选择需提交的文件；不要把缓存、私人配置或无关本地文件一起提交。
- ChatGPT 项目通过可用的 GitHub 工具读取本次所需文件，记录实际读取的分支和提交；工具未提供提交标识时说明版本未确认，不推测提交号。连接不可用时请求提供具体文件内容，不假定已获得最新版本。
- 本流程为按需同步：聊天结论不会自动写入仓库，项目指令不会随仓库文件自动更新。上传的文件副本也需要单独更新。
