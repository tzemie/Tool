# 项目上下文

更新时间：2026-09-15

## 用途与正式资料源

- 仓库：[tzemie/Tool](https://github.com/tzemie/Tool)，正式共享分支：`main`。
- 维护可公开分享的 VPN 客户端配置、分流规则、工具脚本与使用说明。
- 本地工作目录的根目录对应仓库根目录；具体本机路径留在本地环境中。
- 仓库当前为公开状态，私人连接信息与个人上下文不进入此文档。

## 文件索引

| 路径 | 用途 |
| --- | --- |
| `AGENTS.md` | 维护规则，修改前先读取 |
| `PROJECT_CONTEXT.md` | 已确认决定、实施状态和后续工作 |
| `CHATGPT_PROJECT_INSTRUCTIONS.md` | 可复制到 ChatGPT 项目指令的引导文本 |
| `Loon/` | 现有 Loon 配置与 `Rules/` 分流规则 |
| `Quantumultx/` | 现有 Quantumult X 配置 |

## 已确认决定

- 使用现有 `tzemie/Tool` 仓库共享规则、脚本和适合公开的讨论结论。
- 保留现有客户端目录和公开文件路径；新增工具按其用途单独组织。
- 维护约定集中在 `AGENTS.md`；ChatGPT 项目指令引用规则来源，避免再维护一整份重复规则。
- 每次形成有用结论后，将已确认内容整理到此文档；脚本变更在本地实施并完成相应检查。
- 同步采用显式读取、Git 提交和推送。两端产生不同修改时先比较和合并。

## 当前状态与验证边界

- 已核验仓库存在，默认分支为 `main`，当前可见性为公开。
- 本地工作目录已获取仓库现有 8 个文件，并关联 `origin/main`。
- 已准备维护规则、上下文、ChatGPT 项目指令文本和 Git 忽略规则。
- 本次接入保留原有配置与规则内容；没有对其连接效果、客户端兼容性或全部敏感信息作出验证结论。
- 文档的最新远端版本以实际成功推送的提交为准；本文件不作为 ChatGPT 项目已连接或已配置的证明。
- 本流程不提供后台实时同步，也不会自动把聊天记录转成仓库文件。

## 使用流程

1. 开始工作时读取 `AGENTS.md`、本文件和相关配置，确认所用分支及版本。
2. 将讨论中的已确认要求与未决建议分开；只有适合公开的内容进入仓库。
3. 在本地完成修改和相应检查，更新实施状态与验证结果。
4. 检查差异和待提交文件，提交并推送；核对远端提交是否一致。
5. ChatGPT 下次处理相关工作时重新读取仓库文件；使用上传副本时，单独更新副本。

## 待办

- 在目标 ChatGPT 项目中配置指向本仓库的指令，并从该项目实际读取这些文件以验证可用性。
- 根据具体需求逐项维护现有客户端配置与工具。

## 后续结论记录格式

每项结论简要记录：日期、问题、已确认决定、实施状态、验证结果、待办。不要粘贴完整聊天或私人连接信息。
