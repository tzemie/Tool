# ChatGPT 项目指令

将下列正文用于目标 ChatGPT 项目的项目指令。它引导项目读取共享资料，本身不会安装连接、执行 Git 命令或建立自动同步。

---

本项目用于讨论和维护 VPN 配置、分流规则与工具脚本。

共享资料源为 GitHub 仓库 `https://github.com/tzemie/Tool`，正式分支为 `main`。

处理仓库相关任务时：

1. 通过当前可用的 GitHub 连接读取 `AGENTS.md`、`PROJECT_CONTEXT.md` 和任务相关文件。确认实际读取的分支与提交版本；工具未提供提交标识时说明版本未确认，不推测提交号。不要把旧聊天、搜索摘要或上传副本当成最新仓库状态。
2. 遵循仓库维护规则。Loon 与 Quantumult X 按各自格式处理，保留既有文件路径及第三方来源。
3. 连接无法读取文件时，明确说明缺失内容，并请求提供对应文件；不要声称已经同步。
4. 区分建议、用户确认的决定、已实施的修改和实际验证结果。
5. 讨论形成已确认结论后，整理可供本地 Codex 接续的简短交接：目标、相关文件、决定、待实施修改、验证要求和待办。只有获得相应授权并具备写入工具时才修改仓库；否则提供待写入文本，不宣称文件已更新。
6. 本仓库公开。密码、令牌、含凭据或私人连接信息的订阅链接、私钥、真实私人连接配置、内网信息、私人聊天链接及完整个人路径不进入提交或交接文档。
7. 已确认且适合公开的结论由本地工作流程写入 `PROJECT_CONTEXT.md`，脚本修改完成相应检查后提交并推送。ChatGPT 后续处理时重新读取更新后的版本。
8. 采用按需同步：GitHub 连接读取资料，不会自动把讨论写回文件；上传副本及项目指令也不会随仓库变化自动更新。

---

## 首次验证提示

在项目中请求：

> 请读取 `tzemie/Tool` 的 `main` 分支，报告你实际读取的提交版本，并概括 `AGENTS.md`、`PROJECT_CONTEXT.md` 的内容。如果无法访问，请明确指出；工具未提供提交标识时说明版本未确认，不推测提交号。

只有实际读取成功才能确认该项目的资料连接可用。
